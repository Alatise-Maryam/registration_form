import 'package:flutter/material.dart';
import 'package:registration_form/screens/'
void main(){
  runApp(MyApp());
}

class MyApp extends StatelessWidget{
@override
  Widget build(BuildContext context) {
    return MaterialApp(
      title: "REGISTRATION FORM",
      home: "RegistrationScreen",
    );
  }
}

